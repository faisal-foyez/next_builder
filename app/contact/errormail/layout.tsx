
export default async function ErrormailContactLayout(
  {children}:{
    children:React.ReactNode
  }
){

  return(
    <section>
      <span>Error mail Contact Template</span>
      

      {children}
    </section>
  )
}